[package]
name = "process-sender"
version = "0.1.0"
edition = "2021"

[dependencies]
dashboard-shared = { path = "../shared" }
sysinfo = "0.38"
reqwest = { version = "0.12", features = ["json"] }
tokio = { version = "1", features = ["full"] }
